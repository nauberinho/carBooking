/**
 * Created by naube on 2017-09-28.
 */

import React, { Component } from 'react';

export default class LogIn extends Component {
    render(){
        return(
            <div>HOME

            <button id='home' onClick={this.props.changeView}>Go Back</button></div>
        )
    }
}